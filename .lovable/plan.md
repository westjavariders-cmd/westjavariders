# Replace the disclaimer text under "Add to cart"

## Change
In `src/components/public/ConfiguratorForm.tsx` (line 517), replace the current disclaimer under the "Add to cart" button:

- **From:** `Prices are indicative until your booking is confirmed.`
- **To:** `Prices set in IDR (RP). Your bank sets the final exchange rate.`

## Scope
- Single text change in the price card of the configurator.
- No other files, no styling, no logic.
